# 🚀 Руководство по запуску Alliance RuStore

## Быстрый старт

### Запуск приложения

Дважды кликните на файл в корне проекта:

```
start-app.bat
```

Или запустите из командной строки:

```bash
.\start-app.bat
```

### Остановка приложения

Дважды кликните на файл:

```
stop-app.bat
```

Или запустите из командной строки:

```bash
.\stop-app.bat
```

---

## 📋 Что происходит при запуске

1. **Проверка зависимостей**
   - ✅ Проверяется наличие Python 3.12+
   - ✅ Проверяется наличие Node.js 18+
   - ✅ Проверяется виртуальное окружение Python
   - ✅ Проверяются зависимости Node.js

2. **Автоматическая установка** (при необходимости)
   - 📦 Создание виртуального окружения Python
   - 📦 Установка зависимостей Python из `requirements.txt`
   - 📦 Установка зависимостей Node.js из `package.json`

3. **Запуск сервисов**
   - 🐍 Backend (FastAPI) - открывается в новом окне терминала
   - ⚛️ Frontend (Vite + React) - открывается в новом окне терминала

---

## 🌐 URL адреса

После запуска приложение будет доступно по адресам:

- **Frontend (React)**: http://localhost:5173
- **Backend API**: http://localhost:8000
- **API Documentation**: http://localhost:8000/docs
- **API Alternative Docs**: http://localhost:8000/redoc

---

## 📁 Структура проекта

```
c:\dev\alliance\
├── start-app.bat          # 🚀 Запуск всего приложения
├── stop-app.bat           # 🛑 Остановка всего приложения
├── LAUNCH_GUIDE.md        # 📖 Это руководство
├── app\                   # Frontend (React + Vite)
│   ├── src\
│   ├── package.json
│   └── vite.config.js
└── backend\
    └── ars\               # Backend (FastAPI)
        ├── main\
        ├── venv\
        └── requirements.txt
```

---

## 🔧 Требования к системе

### Обязательные требования:

1. **Python 3.12+**
   - Скачать: https://www.python.org/downloads/
   - Убедитесь, что Python добавлен в PATH

2. **Node.js 18+**
   - Скачать: https://nodejs.org/
   - Включает npm автоматически

3. **MySQL Server**
   - База данных должна быть запущена
   - Настройки в `backend/ars/main/config.py`
   - По умолчанию: `rustore2` на `localhost:3306`

---

## ⚙️ Ручной запуск (альтернатива)

### Backend (FastAPI)

```bash
cd backend/ars
call venv\Scripts\activate.bat
python -m main.main
```

### Frontend (Vite + React)

```bash
cd app
npm run dev
```

---

## 🛠️ Решение проблем

### Проблема: "Python не найден"

**Решение:**
1. Установите Python 3.12 или выше
2. Убедитесь, что Python добавлен в PATH
3. Перезапустите терминал

### Проблема: "Node.js не найден"

**Решение:**
1. Установите Node.js 18 или выше
2. Перезапустите терминал

### Проблема: "Ошибка подключения к базе данных"

**Решение:**
1. Убедитесь, что MySQL Server запущен
2. Проверьте настройки в `backend/ars/main/config.py`
3. Проверьте доступ к базе данных `rustore2`

### Проблема: "Порт уже используется"

**Решение:**
1. Запустите `stop-app.bat` для остановки всех процессов
2. Или вручную закройте процессы на портах 8000 и 5173
3. Перезапустите приложение

### Проблема: "CORS ошибки"

**Решение:**
1. Убедитесь, что бэкенд запущен на порту 8000
2. Проверьте `backend/ars/main/config.py` - порт 5173 должен быть в CORS_ORIGINS
3. Перезапустите бэкенд

---

## 📝 Логи и отладка

### Просмотр логов Backend:
- Логи отображаются в окне терминала Backend
- Уровень логирования можно изменить в `backend/ars/main/config.py`

### Просмотр логов Frontend:
- Логи отображаются в окне терминала Frontend
- Также доступны в консоли браузера (F12)

---

## 🔄 Обновление зависимостей

### Backend (Python):

```bash
cd backend/ars
call venv\Scripts\activate.bat
pip install -r requirements.txt
```

### Frontend (Node.js):

```bash
cd app
npm install
```

---

## 📚 Дополнительные ресурсы

- **FastAPI документация**: https://fastapi.tiangolo.com/
- **React документация**: https://react.dev/
- **Vite документация**: https://vitejs.dev/

---

## 💡 Полезные команды

### Открыть в браузере после запуска:

```bash
start http://localhost:5173
```

### Проверить статус портов:

```bash
netstat -ano | findstr "8000"
netstat -ano | findstr "5173"
```

### Принудительная остановка процесса:

```bash
# Остановить процесс на порту 8000
for /f "tokens=5" %a in ('netstat -aon ^| find ":8000" ^| find "LISTENING"') do taskkill /F /PID %a

# Остановить процесс на порту 5173
for /f "tokens=5" %a in ('netstat -aon ^| find ":5173" ^| find "LISTENING"') do taskkill /F /PID %a
```

---

## ✅ Успешный запуск

После успешного запуска вы увидите:

1. ✅ Два окна терминала открыты
2. ✅ Backend работает на http://localhost:8000
3. ✅ Frontend работает на http://localhost:5173
4. ✅ Браузер автоматически откроется на странице приложения

**Приятной работы!** 🎉



