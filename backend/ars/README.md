# Rustore API Backend

Бэкенд API для магазина приложений на базе FastAPI и MySQL.

## 📁 Структура проекта

```
backend/ars/
├── main/                      # Основной пакет приложения
│   ├── __init__.py           # Инициализация пакета
│   ├── main.py               # FastAPI приложение и API endpoints
│   ├── config.py             # Конфигурация, пути, константы
│   ├── database.py           # Подключение к БД, сессии
│   ├── models.py             # SQLAlchemy модели (таблицы БД)
│   ├── schemas.py            # Pydantic схемы для API
│   └── seed.py               # Заполнение БД тестовыми данными
├── static/                    # Статические файлы (на уровне ars/)
│   ├── screenshots/          # Скриншоты приложений (.webp)
│   │   ├── sber_1.webp      # По 3 скриншота на каждое приложение
│   │   ├── tbank_1.webp
│   │   └── ...              # Всего 27 файлов
│   └── icons/                # Иконки приложений (.webp)
│       ├── sber.webp        # По 1 иконке на приложение
│       ├── tbank.webp
│       └── ...              # Всего 9 файлов
├── README.md                  # Документация проекта
└── requirements.txt          # Python зависимости
```

## 🏗️ Архитектура приложения

```
┌─────────────────────────────────────────────────────────┐
│                      Frontend (React)                    │
│              http://localhost:3000                       │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP/REST API
                     ↓
┌─────────────────────────────────────────────────────────┐
│                 main.py (FastAPI App)                    │
│  • API Endpoints                                         │
│  • CORS Middleware                                       │
│  • Static Files Mounting                                 │
└───┬──────────────┬──────────────┬──────────────┬────────┘
    │              │              │              │
    ↓              ↓              ↓              ↓
┌─────────┐  ┌──────────┐  ┌─────────┐  ┌──────────────┐
│config.py│  │schemas.py│  │models.py│  │  database.py │
│• Paths  │  │• Pydantic│  │• AppDB  │  │• Engine      │
│• DB URL │  │  Models  │  │• ScreenDB│ │• SessionMaker│
└─────────┘  └──────────┘  └────┬────┘  └──────┬───────┘
                                 │               │
                                 └───────┬───────┘
                                         ↓
                            ┌────────────────────────┐
                            │   MySQL Database        │
                            │   • apps table          │
                            │   • screenshots table   │
                            └────────────────────────┘
                                         ↑
                                         │
                                    ┌────┴────┐
                                    │ seed.py │
                                    │ (init)  │
                                    └─────────┘
```

## 🗂️ Описание модулей

### `config.py`
- Конфигурация приложения
- Пути к статическим файлам (иконки, скриншоты)
- Строка подключения к MySQL
- CORS origins для фронтенда
- Проверка существования статических файлов

### `database.py`
- Настройка SQLAlchemy engine
- Создание сессий БД
- Dependency `get_db()` для FastAPI
- Функция создания таблиц

### `models.py`
- SQLAlchemy модели:
  - `AppDB` - таблица приложений
  - `ScreenshotDB` - таблица скриншотов
- Relationships между таблицами

### `schemas.py`
- Pydantic схемы для валидации:
  - `App` - схема приложения для API
  - `Screenshot` - схема скриншота

### `seed.py`
- Функция `seed_data()` для заполнения БД
- Тестовые данные для 9 приложений
- Автоматическое создание связей app ↔ screenshots

### `main.py`
- Инициализация FastAPI приложения
- CORS middleware
- Монтирование статических файлов
- API endpoints:
  - `GET /` - приветствие
  - `GET /health` - health check
  - `GET /debug/files` - диагностика файлов
  - `GET /api/apps` - список приложений
  - `GET /api/apps/{id}` - детали приложения
  - `GET /api/categories` - список категорий
  - `GET /api/search?q=...` - поиск приложений
  - `GET /api/featured` - топ-3 приложения

## 🗄️ База данных

### Таблица `apps`
| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Primary key |
| name | String(100) | Название приложения |
| developer | String(100) | Разработчик |
| category | String(50) | Категория |
| age_rating | String(10) | Возрастной рейтинг |
| description | Text | Описание |
| icon_url | String(255) | Путь к иконке |
| rating | Float | Рейтинг (0-5) |
| version | String(20) | Версия |
| size | String(20) | Размер |
| price | String(50) | Цена |
| last_update | Date | Дата обновления |

### Таблица `screenshots`
| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Primary key |
| app_id | Integer | Foreign key → apps.id |
| image_url | String(255) | Путь к скриншоту |

## 🚀 Установка и запуск

### Требования
- Python 3.8+
- MySQL 8.0+
- Виртуальное окружение (рекомендуется)

### 1. Установка зависимостей

```bash
# Переход в папку бэкенда
cd backend/ars

# Создание виртуального окружения (опционально, но рекомендуется)
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка базы данных

```bash
# Войти в MySQL
mysql -u root -p

# Создать базу данных
CREATE DATABASE rustore2;
EXIT;
```

**Важно:** Проверьте настройки подключения в `main/config.py`:
```python
DATABASE_URL = "mysql+mysqlconnector://root:1111@127.0.0.1:3306/rustore2"
```
Измените `root` (пользователь), `1111` (пароль) и `rustore2` (имя БД) на свои данные при необходимости.

**Текущие настройки:**
- Пользователь: `root`
- Пароль: `1111`
- Хост: `127.0.0.1`
- Порт: `3306`
- База данных: `rustore2`

### 3. Добавление статических файлов

Убедитесь, что в папках есть файлы:
- `static/icons/` - 9 иконок (.webp)
- `static/screenshots/` - 27 скриншотов (.webp)

Необходимые файлы:
```
icons:
  - sber.webp, tbank.webp, vtb.webp, gos.webp
  - avito.webp, ozon.webp, 2gis.webp
  - minibank.webp, mail.webp

screenshots:
  - sber_1/2/3.webp, tbank_1/2/3.webp
  - vtb_1/2/3.webp, gos_1/2/3.webp
  - avito_1/2/3.webp, ozon_1/2/3.webp
  - 2gis_1/2/3.webp, minibank_1/2/3.webp
  - mail_1/2/3.webp
```

### 4. Запуск сервера

**Вариант 1: Используя готовые скрипты (рекомендуется)**

```bash
# Windows:
start.bat

# Linux/Mac:
chmod +x start.sh
./start.sh
```

**Вариант 2: Вручную через uvicorn**

```bash
# Из папки backend/ars
uvicorn main.main:app --reload --host 0.0.0.0 --port 8000
```

**Вариант 3: Напрямую через Python**

```bash
python -m main.main
```

### 5. Автоматическое заполнение БД

При первом запуске:
1. ✅ Автоматически создаются таблицы `apps` и `screenshots`
2. ✅ Автоматически заполняются тестовыми данными (9 приложений)
3. ✅ Создаются связи между приложениями и скриншотами

Если БД уже содержит данные, повторное заполнение **не происходит**.

### 6. Проверка работы

Откройте в браузере:
- API документация: http://localhost:8000/docs
- Health check: http://localhost:8000/health
- Список приложений: http://localhost:8000/api/apps
- Диагностика файлов: http://localhost:8000/debug/files

## 🔧 Разработка

### Очистка БД для повторного заполнения

```bash
mysql -u root -p

USE rustore2;
DELETE FROM screenshots;
DELETE FROM apps;

# Или полное удаление таблиц
DROP TABLE screenshots;
DROP TABLE apps;
EXIT;
```

После этого перезапустите сервер - таблицы создадутся заново.

### Изменение тестовых данных

Отредактируйте файл `main/seed.py`, функцию `seed_data()`.

### Добавление новых endpoints

Добавьте новые функции в `main/main.py` с декораторами `@app.get()`, `@app.post()` и т.д.

## 🌐 API Endpoints

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | `/` | Приветственное сообщение |
| GET | `/health` | Проверка статуса сервера |
| GET | `/debug/files` | Диагностика статических файлов |
| GET | `/api/apps` | Список всех приложений |
| GET | `/api/apps?category=Финансы` | Фильтр по категории |
| GET | `/api/apps/{id}` | Детали приложения по ID |
| GET | `/api/categories` | Список всех категорий |
| GET | `/api/search?q=банк` | Поиск по названию/описанию |
| GET | `/api/featured` | Топ-3 приложения по рейтингу |

## 📦 Зависимости

```txt
fastapi>=0.104.0
uvicorn>=0.24.0
sqlalchemy>=2.0.0
mysql-connector-python>=8.0.0
pydantic>=2.0.0
```

## 🔗 Интеграция с фронтендом

Фронтенд может подключаться с:
- `http://localhost:3000` (стандартный React)
- `http://localhost:5273` (Vite)

CORS настроен автоматически в `config.py`.

## 📝 Логи

При запуске сервер выводит:
- ✅ Найденные статические файлы
- 📊 Созданные таблицы в БД
- 📱 Доступные API endpoints
- 🚀 Адрес сервера

## 🐛 Troubleshooting

### Ошибка подключения к MySQL
```
sqlalchemy.exc.OperationalError: Can't connect to MySQL server
```
**Решение:** Проверьте, что MySQL запущен и данные для подключения в `config.py` верны.

### Статические файлы не загружаются
**Решение:** Убедитесь, что файлы находятся в правильных папках (`static/icons/`, `static/screenshots/`) и имеют расширение `.webp`.

### Таблицы не создаются
**Решение:** Проверьте права доступа к БД и существование базы `rustore2`.

## 👨‍💻 Автор

Разработано для учебного проекта Alliance.

