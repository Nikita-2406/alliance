# 🚀 Быстрый старт

## За 5 минут до запуска

### 1️⃣ Установка зависимостей
```bash
cd backend/ars
pip install -r requirements.txt
```

### 2️⃣ Настройка MySQL
```bash
mysql -u root -p
CREATE DATABASE rustore2;
EXIT;
```

### 3️⃣ Проверка config.py
Откройте `main/config.py` и проверьте строку подключения:
```python
DATABASE_URL = "mysql+mysqlconnector://root:ВАШИ_ПАРОЛЬ@127.0.0.1:3306/rustore2"
```

### 4️⃣ Запуск
```bash
# Windows:
start.bat

# Linux/Mac:
./start.sh
```

### 5️⃣ Проверка
Откройте: http://localhost:8000/docs

## ✅ Что происходит автоматически

1. ✅ Создаются таблицы `apps` и `screenshots`
2. ✅ База заполняется 9 приложениями с данными
3. ✅ Проверяются статические файлы
4. ✅ API готов к работе!

## 📱 API Endpoints

- `GET /api/apps` - все приложения
- `GET /api/apps/1` - детали приложения #1
- `GET /api/categories` - категории
- `GET /api/search?q=банк` - поиск
- `GET /api/featured` - топ-3

## 🔧 Если что-то пошло не так

### Ошибка подключения к MySQL
```bash
# Проверьте что MySQL запущен
# Windows: services.msc → MySQL
# Linux: sudo systemctl status mysql
```

### База уже заполнена, хочу пересоздать
```bash
mysql -u root -p
USE rustore2;
DROP TABLE screenshots;
DROP TABLE apps;
EXIT;
# Перезапустите сервер
```

### Статические файлы не отображаются
Убедитесь что в `backend/ars/static/` есть:
- `icons/*.webp` (9 файлов)
- `screenshots/*.webp` (27 файлов)

## 📖 Полная документация

См. [README.md](README.md) для подробной документации.

