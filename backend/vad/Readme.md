# App Store Reviews API

Это проект бэкенда для витрины приложений с функционалом отзывов. Реализовано на **FastAPI**, **MySQL** и **Python**.

## 🚀 Возможности
- Создание отзывов
- Просмотр отзывов по приложению
- Обновление и удаление отзывов
- Подсчет статистики (количество и средняя оценка)
- Поддержка авторизации по VK ID (базовый вариант)

---

# 📦 Установка и запуск

## 1. Склонируйте проект
```bash
cd alliance/backend/vad
```

---

## 2. Установите зависимости
```bash
pip install -r requirements.txt
```

---

## 3. Создайте файл `.env`
Создайте файл `.env` рядом с `database.py`:

```
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=app_store
```

---

## 4. Запустите MySQL Server
Убедитесь, что MySQL работает.
- **Windows**: через MySQL Workbench или "Службы"
- **Linux**:
```bash
sudo systemctl start mysql
```

---

## 5. Инициализируйте базу данных
```bash
python init_database.py
```
Ожидаем вывод:
```
Database and table created successfully!
```

---

## 6. Запуск сервера
Перейдите в директорию, где находится `main.py`:
```bash
cd alliance/backend/vad
uvicorn main:app --reload
```

В консоли появится:
```
Uvicorn running on http://127.0.0.1:8000
```

---

# 🧪 Тестирование API
FastAPI автоматически создаёт документацию.

## Swagger UI
👉 http://127.0.0.1:8000/docs

## Redoc
👉 http://127.0.0.1:8000/redoc

---

# 📝 Основные эндпоинты

## Создать отзыв
**POST** `/reviews/`
```json
{
  "app_id": 1,
  "vk_id": 123456,
  "nickname": "tester",
  "description": "Отличное приложение!",
  "stars": 5
}
```

## Получить отзывы по приложению
**GET** `/reviews/app/{app_id}`

## Обновить отзыв
**PUT** `/reviews/{review_id}?vk_id=123456`

## Удалить отзыв
**DELETE** `/reviews/{review_id}?vk_id=123456`

## Получить статистику
**GET** `/reviews/stats/{app_id}`

---

# 📘 Структура проекта
```
backend/
└── vad/
    ├── main.py
    ├── database.py
    ├── models.py
    ├── reviews_repository.py
    ├── init_database.py
    ├── requirements.txt
```

---

# 💡 Дополнительно
Если нужно — можно:
- добавить полноценную OAuth авторизацию через VK
- подключить фронтенд
- развернуть API на сервере

Готов помочь с расширением проекта!